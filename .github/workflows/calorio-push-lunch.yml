name: calorio push · midi
# Rappel de midi (~12:30 heure suisse en été). N'envoie que si rien n'est noté aujourd'hui (géré côté serveur).
on:
  schedule:
    - cron: "30 10 * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -X GET -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" "https://outils.ch/api/push-cron?job=lunch"
